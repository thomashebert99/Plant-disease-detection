"""Evaluation helpers for classification tasks."""

from __future__ import annotations


def evaluate_model(model_name: str) -> dict[str, float | str]:
    """Return placeholder evaluation metrics."""
    return {"model_name": model_name, "accuracy": 0.0}
